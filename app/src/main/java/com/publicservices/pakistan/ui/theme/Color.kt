package com.publicservices.pakistan.ui.theme

import androidx.compose.ui.graphics.Color

// Pakistan Theme Colors - Vibrant Green for Sahulat V3
val PakistanGreen = Color(0xFF28C52E) // New Primary Vibrant Green
val LightGreen = Color(0xFF4ADE52) // Brighter variant
val DarkGreen = Color(0xFF1B8A20) // Darker variant for contrast

// Neutral Colors - Corrected for visibility
val White = Color(0xFFFFFFFF)
val OffWhite = Color(0xFFF8F9FA) // Brighter off-white for better contrast
val LightGray = Color(0xFFE0E0E0)
val Gray = Color(0xFF455A64) // Darker slate gray for better text visibility in light mode
val DarkGray = Color(0xFF1A1C1E) // Improved dark surface
val Black = Color(0xFF000000)

// Accessibility-focused text colors
val OnSurfaceLight = Color(0xFF1A1C1E)
val OnSurfaceVariantLight = Color(0xFF49454F) // Much darker for readability
val OnSurfaceDark = Color(0xFFE2E2E6)
val OnSurfaceVariantDark = Color(0xFFC9C6D0)

// Accent Colors
val AccentRed = Color(0xFFB3261E) // Accessible red
val AccentBlue = Color(0xFF0061A4)
val SurfaceVariant = Color(0xFFE1E2EC)
